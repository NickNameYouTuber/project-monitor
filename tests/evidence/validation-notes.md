# Заметки по валидации

## Проверка в редакторе
- Статическая проверка файлов в [newbackend/src/test/java](../../newbackend/src/test/java) не показала IDE-ошибок на момент реализации.

## Результат запуска
1. Первый прямой запуск `mvn -f newbackend/pom.xml test` не выполнился, потому что Maven не был установлен глобально.
2. Docker-вариант запуска был недоступен, так как Docker daemon не был запущен.
3. После этого в [.tools](../../.tools) были скачаны локальные JDK 21 и Maven.
4. Затем тестовый набор был успешно выполнен через локальные бинарные файлы.

## Итоговый статус
- Использованная команда: `mvn -f newbackend/pom.xml test` через локальный Maven из `.tools`
- Результат: `BUILD SUCCESS`
- Всего тестов: `14`
- Failures: `0`
- Errors: `0`
- Skipped: `0`
